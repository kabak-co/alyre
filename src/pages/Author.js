import { string } from 'prop-types';
import React, { PropTypes } from 'react';

const Author = props => {
  return (
    <div>
      
    </div>
  );
};

Author.propTypes = {
  books: Array,
  fullName: string,
};

export default Author;