import React, { PropTypes } from 'react';

const Books = props => {
  return (
    <div>
      Bibliothèque de livres
    </div>
  );
};

Books.propTypes = {
  // book: Array
};

export default Books;