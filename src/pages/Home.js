import React, { PropTypes } from 'react';

const Home = props => {
  return (
    <div>
      Home
      <p>Header</p>
    </div>
  );
};

Home.propTypes = {
  // book: Array,
  // news: Object,
};

export default Home;