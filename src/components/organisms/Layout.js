import React, { PropTypes } from 'react';

const Layout = props => {
  return (
    <div>
      {/* HEADER */}
      {/* CONTENT : qui changera selon la page donc un router sera nécessaire */}
    </div>
  );
};

Layout.propTypes = {
  
};

export default Layout;